pipeline {
    agent any

    stages{
        stage('initWorkspace') {
            steps{
                
            }
        }
    }

    post {
        always {
            deleteDir()
        }
    }

}